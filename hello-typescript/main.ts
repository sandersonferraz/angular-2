var myVar = 'variable me';

function myFunc(x, y) {
    return x + y;
}

// ES 6 or ES 2015
let nom = 2; // variable declaration.
const PI = 3.14; // Constant declaration.

var number = [1, 2, 3];

//Map function
number.map(function (value) {
    return value * 2;
});

// ES 2015
number.map(valuer =>
    valuer * 2
);

class Mathematic {

    sum(x, y) {
        return x + y;
    }

}

// Variable typing
var n1: string = 'Text';



